spring:
  application:
    name: razorpay

  datasource:
    url: jdbc:postgresql://localhost:5432/razorpay
    username: niraj
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
